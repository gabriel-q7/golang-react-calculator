The monorepo structure, shared tooling, Docker configuration, `Makefile`, and backend API are already in place. Review and adhere to the existing architectural, organizational, and coding decisions rather than introducing new patterns or duplicate configuration. 

Build the frontend using React, TypeScript, Tailwind CSS, and shadcn/ui within the established project structure. Organize the code by feature, keeping presentation, validation, and API communication concerns separate. Consume the existing backend REST API for all calculator operations, providing intuitive forms for each operation, clear display of results and errors, and client-side validation before making requests. 

Ensure the UI is responsive with basic mobile support, integrate with the existing Docker and Makefile workflows, add unit tests with coverage reporting for key components and interactions, and update the `README.md` with any necessary frontend usage information.
