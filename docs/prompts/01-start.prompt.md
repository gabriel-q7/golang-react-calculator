The monorepo folder and an initial `README.md` already exist. Initialize the project structure inside this repository without recreating or overwriting those items. 

Add the directories and configuration needed for a React + TypeScript frontend and a Go backend, create a `docs/` folder for architecture and API documentation, and update the existing `README.md` with setup instructions, the overall architecture, and design rationale. 

Configure Docker and Docker Compose for local development, add a root `Makefile` with targets such as `build`, `up`, `down`, `test`, `coverage`, and `clean`, and prepare a production-ready multi-stage `Dockerfile` that will ultimately run the complete application in a single container.
