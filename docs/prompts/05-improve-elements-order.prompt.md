Refine the connected hexagonal keypad layout to follow the conventional arrangement of a physical calculator. The numeric keys (`7–9`, `4–6`, `1–3`, and `0`) should form a contiguous block on the left and center of the keypad, preserving the connected honeycomb appearance. Move the primary arithmetic operators (`÷`, `×`, `−`, `+`) into a dedicated rightmost column, and place secondary actions (such as `=`, clear, percentage, square root, and exponentiation) in the bottom row. Maintain the edge-to-edge connection between all hexagons, preserve the vaporwave theme and responsiveness, and ensure that the revised layout remains accessible, testable, and fully compatible with the existing backend API.
